# OCR 剪贴工具

一个 Windows 上的个人工具：按热键框选屏幕区域，把选区截图交给多模态大模型，取回其中的文字，并可就这段文字继续做翻译、总结等处理。

## Language

**截图（Capture）**:
用户按热键后框选出来的一块屏幕区域及其图像。识别与后续处理的输入都是截图。

**识别（Recognize）**:
从一张截图中取出其中的文字，得到**文本**。本项目里识别由多模态大模型完成，不经过传统 OCR 引擎。

**文本（Text）**:
一次识别得到的纯净文字内容。它是**结果窗口**里可编辑的主体，也是所有**动作**的输入。

**动作（Action）**:
对**文本**做的一次加工，由用户在结果窗口里主动点按钮触发。共四种：翻译成中文、总结、表格转换、自定义提问。
_Avoid_: 处理、操作

**自定义提问（Ask）**:
一种动作：用户自己输入一句指令，连同**文本**一起交给大模型，回答回到同一个结果窗口。是唯一由用户决定内容的动作。

**结果窗口（Result Window）**:
截图识别后弹出的那一个小窗口。上半区是可编辑的**文本**，下半区显示**动作**的答案，底部内置自定义提问的输入框。

**模型（Model）**:
用户配置的那一个大模型服务，通过 OpenAI 兼容接口访问。识别和各动作都由它承担，全局只有一个。
_Avoid_: AI、LLM、接口

**设置（Settings）**:
从托盘打开的配置入口，管三件事：模型（地址、Key、模型名）、热键、开机自启开关。

**热键（Hotkey）**:
全局快捷键，默认 Ctrl+Alt+O，按下即进入截图模式。
